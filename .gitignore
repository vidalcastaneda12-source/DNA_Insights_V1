# Runtime data and archives (never commit)
data/
archive/
*.txt

# Verification scratch — TMPDIR for scripts/verify.sh; cleared each run.
.verify-tmp/

# Tabix index files (cyvcf2 caches these in cwd when opening remote VCFs;
# they're regenerable on demand and never part of source).
*.tbi
*.bgz
*.bgz.tbi

# Environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
*.egg-info/
build/
dist/
pip-log.txt

# uv
uv.lock

# Frontend
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
Thumbs.db

# Claude Code (per-user local config and runtime state)
.claude/*
# Slash commands are shared project infrastructure — track them in the repo.
!.claude/commands/
# The per-scope agent team is shared, reviewable infrastructure (finding-034).
!.claude/agents/
!.claude/hooks/
!.claude/workflows/
!.claude/settings.json
hg19ToHg38.over.chain.gz:Zone.Identifier
hg19ToHg38.over.chain.gz
genome_Vidal_Castaneda_v5_Full_20260503061119.txt:Zone.Identifier
AncestryDNA.txt:Zone.Identifier
